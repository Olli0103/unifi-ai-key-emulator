# Set these to the account that owns this project's state directory on the NAS.
AIKEY_UID=10001
AIKEY_GID=10001
# Replace this documentation placeholder with the exact console LAN address.
# Needed by the optional PostgreSQL profile.
CONSOLE_IP=192.0.2.1
