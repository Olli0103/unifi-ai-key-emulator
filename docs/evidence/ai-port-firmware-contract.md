# AI Port firmware and first native discovery result

This is an interface record for issue [#6](https://github.com/Olli0103/unifi-ai-key-emulator/issues/6). It does not include vendor code, firmware, credentials or camera media. The existing-device reconnect result below does not prove fresh management-token adoption.

## Provenance

[Ubiquiti's public firmware catalog](https://fw-update.ui.com/api/firmware/1dd26036-3ab7-4e12-bdbb-dc5b5ca9ccfe) identifies product `AI-Port`, platform `all`, version `5.1.12`, with SHA-256 `4bdd8d21533cf1b95746d02fd5a63ab563d320f10c73041ef8be03673dcd3f95`. The package from the catalog's [official download URL](https://fw-download.ubnt.com/data/AI-Port/4dcc-all-5.1.12-8f4dc4b3-0aa7-4c3f-90d3-8aa4c568f29b.bin) matched that hash. It was inspected as data in temporary storage. No vendor binary was executed or added to this repository.

| Interface | Evidence | Limit |
| --- | --- | --- |
| AI Port identity is a separate `aiport` model, named `UVC AI Port`, with sysid `0xa5f1`. | Protect 7.2.105 controller modules `53631`, `19157` and `4973` | Live Protect 7.3.60 accepted the candidate and later accepted an existing-device adopted reconnect; fresh adoption is `needs_evidence`. |
| The bundled web server listens on HTTPS TCP 443 and routes `/api/*` to its management CGI. The current controller posts adoption data to `/api/1.2/manage`. | AI Port 5.1.12 rootfs `/etc/lighttpd.conf` and `/usr/etc/lighttpd/lighttpd.conf`; Protect module `42278` and camera request module `16436` | Exact management response and credential-rotation lifecycle are `needs_evidence`. |
| AI Port's camera client uses the `/camera/1.0/ws` path and `secure_transfer` WebSocket subprotocol. | AI Port 5.1.12 `ubnt_avclient` strings; Protect modules `19157` and `71420` | Message framing, settings and event payloads are `needs_evidence`. |
| The firmware includes the usual UDP 10001 multicast discovery responder. | AI Port 5.1.12 `ubntbox.static` interface strings | The native candidate test below did not require that responder; its necessity for reconnect or other networks is `needs_evidence`. |
| One outbound, certificate-pinned, unadopted WebSocket upgrade to the controller's device service on TCP 7442 returned HTTP 101. The subsequent Protect 7.3.60 Devices view showed a separate AI Port at the probe's advertised address with status **Click to Adopt** while the existing AI Key remained Online. | Bounded local trial on 23 September 2026; pre- and post-trial read-only Protect API and Devices view | This proves candidate registration only. It does not prove adoption, connection, stream ingestion, detection, or camera pairing. |
| An isolated candidate container held the pinned WebSocket open and answered certificate-validated HTTPS health checks on a temporary host TCP 8443. Protect showed **Click to Pair** and **Connecting**, but a legacy-camera pairing attempt returned **Unable to Pair**. No management request reached the temporary 8443 listener. | Bounded Mac trial and Protect screenshots on 23 September 2026 | The device's fixed host HTTPS 443 endpoint was unavailable. This early trial did not prove pairing or adoption. |
| A later ten-minute, controller-source-restricted TLS relay made host TCP 443 reachable for one legacy-camera pairing retry. The relay saw zero accepted connections from the console; its one rejected connection was the local reachability probe. The candidate still saw zero management requests. | Relay aggregate counters and certificate-verified candidate health on 23 September 2026 | Opening 443 alone did not fix this attempt. The 7.2.105 controller pairing code issues `UiStreamControl` through the existing camera WebSocket, while the management `POST` is part of adoption. The later 7.3.60 live trial confirmed the stream-control path. |
| A five-minute, opt-in diagnostic sent only the initial `ubnt_avclient_hello` and acknowledged `ubnt_avclient_paramAgreement` over the pinned WebSocket. Protect replied to the hello, sent parameter agreement and `GetStreamList`, and then showed the separate AI Port **Online** in its device detail while the AI Key remained separate. | Candidate's payload-free counters and read-only Protect Devices view on 23 September 2026 | The initial control handshake is native-verified on Protect 7.3.60. The candidate did not answer `GetStreamList`, adopt through management, ingest a stream, pair a camera, or generate detections. Online status alone is not feature parity. The diagnostic automatically closes its WebSocket at the configured deadline and does not send a new hello after expiry. |
| The diagnostic subsequently counted a `UiStreamControl` command. After expiry, the candidate reconnected passively and Protect returned to **Connecting** / **Click to Pair**. | Payload-free control counters and read-only Protect Devices view on 23 September 2026 | The deployed candidate did not acknowledge stream control or ingest media. A missing stream-control response is consistent with the failed pairing; Protect's exact failure reason is `needs_evidence`. A newer offline-tested build rejects unsupported stream control explicitly, but was not deployed for this trial. |
| The failed Save left the tested camera checked in the open Pair Camera dialog. After closing and reopening the dialog, no camera was selected and Save was disabled; the camera remained **Online**. | User-supplied failure screenshot and later read-only Protect Devices view on 23 September 2026 | The checkbox in the failed dialog was a pending UI selection, while the reopened dialog shows no retained pairing. A complete camera-settings rollback was then `needs_evidence` because that trial had no pre-trial baseline. Later trials used a separate stream receiver and recorded a baseline before retrying. |
| A separately built candidate answered `GetStreamList` during a bounded live control trial. The certificate-verified health endpoint recorded 15 hello replies and 15 stream-list responses before the gate expired, with zero stream starts and zero decoded frames. Its private configuration was restored to passive mode, and a subsequent certificate-verified health check showed ingress disabled. | Payload-free counters from the isolated candidate and a read-only Protect camera-settings baseline on 23 September 2026 | This proves the read-only stream-list response and expiry behavior. No camera was paired or opened, so native `UiStreamControl`, RTSP decoding, camera rollback, and smart-event delivery remain `needs_evidence`. |
| A later bounded, one-camera trial received `UiStreamControl`. The command passed the exact camera/source/port admission checks and started FFmpeg, which exited with status 1 before decoding a frame. Its error text was reduced to a fixed `stream_ended` code and discarded; no alias or media was retained. Protect returned to **Click to Pair**. The tested camera remained Online with its previous recording resolution, mode and secure stream settings. The candidate was restarted with stream ingress disabled, while the separate AI Key stayed Online. | Certificate-verified candidate health counters, Protect Devices and camera-settings views, and a private pre-trial baseline on 23 September 2026 | Native stream-control delivery and rollback were observed at this point. The decoder's specific error and successful pairing were still `needs_evidence` until the later trial below. |
| A subsequent one-camera trial classified the FFmpeg failure using only fixed terms: `option`, `timeout`, `not`, `found`. The decoder had used `-rw_timeout`; the container's RTSP demuxer lists `-timeout` as its socket I/O option. After changing the flag, a bounded Protect trial returned a successful `UiStreamControl` response, decoded at least ten frames, and showed the legacy camera paired to the separate AI Port. The camera was then unpaired in Protect, returning the AI Port to **Click to Pair**. The candidate restarted with ingress disabled, the camera remained Online with its prior recording and secure stream settings, and the separate AI Key remained Online. | Fixed-vocabulary candidate health, container FFmpeg option listing, Protect Devices and camera-settings views, and private pre-trial baseline on 23 September 2026 | One-camera pairing and media ingress are observed. This does not prove model inference, event delivery, retention or durable operation. Those remain `needs_evidence`; the test ended with no camera paired. |
| A follow-up bounded trial paired the same legacy camera again and decoded frames. The candidate counted five WebSocket upgrades, five hello replies, five parameter agreements, five `GetStreamList` commands and one successful `UiStreamControl`. It received 38 binary frames in all. None of the fixed, allowlisted smart-settings function names appeared. The camera was unpaired again and the candidate restarted passively. Protect still showed the camera and the separate AI Key Online, with the camera's recording and secure stream settings intact. | Payload-free function counters, pinned candidate health and read-only Protect views on 23 September 2026 | Smart-service readiness and the reason for reconnects remain `needs_evidence`. The other binary frames were not classified in this trial, so absence of an allowlisted name does not prove that Protect sent no other settings command. |
| A later bounded retry initially failed at FFmpeg with the fixed label `rtsp_invalid_data`. A delayed retry of the same one-camera pairing succeeded and decoded at least ten frames. Protect showed the camera paired, then showed **Click to Pair** after unpairing. Repeated control connections closed with WebSocket code `1000`, including after returning the candidate to passive mode. During the paired interval, no fixed-allowlist smart-settings command was observed; other function names were counted without recording them. | Pinned candidate health counters and read-only Protect Devices view on 23 September 2026 | Video ingress is possible but startup is intermittent. Close code `1000` does not identify why the peer or transport closed, and the absence of a known smart-settings command does not establish smart-service behavior. Event delivery remains `needs_evidence`. |
| The candidate later sent one `EventAIPortStatus` over the existing control WebSocket after decoding the first frame for the same bounded camera. It set streaming true while reporting smart-detection and audio-event readiness false. Protect showed the camera paired and Online. The control connection still closed with code `1000` and reconnected. The camera was unpaired and the private config restored to passive mode; the separate AI Key stayed Online. | Pinned candidate health counters and read-only Protect Devices view on 23 September 2026 | This proves the status event was sent and the camera still paired. Protect did not expose an acknowledgement that it processed the event; that and the reconnect cause remain `needs_evidence`. No smart detection or event delivery was claimed. |
| A later bounded trial paired the legacy camera again. A revised candidate preserved the active decoder across one normal control WebSocket reconnect and answered the next stream-list request with that stream. It decoded at least 19 frames and Protect displayed the AI Port Online with the camera paired. Protect then sent `ResetAIPortStreams`, which this deployed build counted but did not answer. The camera was unpaired, the original private config restored, and the candidate restarted with ingress disabled. | Pinned, payload-free health counters and Protect Devices view on 23 September 2026 | The 15-second reconnect grace works live. It is not a permanent stream lifecycle. Protect 7.2.105 static controller code calls `ResetAIPortStreams` when its connected-state stream list is nonempty; a new synthetic test proves the candidate's reset response and restart path, but live validation of that response is `needs_evidence`. |
| The next bounded trial used a candidate that closes its decoder and replies with status `0` to `ResetAIPortStreams`. Protect showed the AI Port Online with the legacy camera paired. The candidate decoded at least 14 frames before one reconnect and sent one reset response. Over the next two WebSocket reconnects, it received no second `UiStreamControl` start; the decoder remained stopped. The camera was unpaired and remained Online at its previous HD/Adaptive settings. The separate AI Key remained Online, and the candidate restarted passively with zero frames retained. | Pinned, payload-free health counters and read-only Protect Devices view on 23 September 2026 | Sending the reset response did not establish durable streaming. Whether the controller processed it, what the remaining unclassified frames mean, and why it did not request a new stream are `needs_evidence`. No smart detection was delivered. |
| A hello-only, no-camera probe then counted three `ChangeVideoSettings` requests and no `ChangeIspSettings` request when neither was answered. A revised probe answered one empty video-settings query with a minimal no-camera profile; Protect next sent and received one image-settings query. The probe had no camera ingress and was returned to passive mode. | Pinned, payload-free function and reply counters on 23 September 2026; Protect 7.2.105 static provisioning order | The unanswered video-settings request was a confirmed provisioning gate. The content of the later unclassified requests and whether Protect marked the candidate provisioned remain `needs_evidence`. |
| A third hello-only, no-camera probe acknowledged `StopService` for SSH, which is absent from the candidate container. Protect then sent `UpdateUsernamePassword`; the candidate counted that fixed command name but did not log its payload or claim to have changed credentials. It was returned to passive mode with no camera access. | Pinned, payload-free function counters on 23 September 2026; Protect 7.2.105 AI Port connected-state handler | Password rotation is the next confirmed gate. The candidate must persist and enforce the new credential across restart before acknowledging it. Subsequent controller commands and durable streaming remain `needs_evidence`. |
| A fourth hello-only, no-camera probe used an independently implemented private credential store. It atomically persisted one syntactically valid SHA-512 crypt hash and sent a status-0 `UpdateUsernamePassword` reply. Protect then sent `ChangeSoundLedSettings`. The private file had mode 600, no payload was logged or published, and the candidate restarted passively with no camera paired. | Pinned, payload-free counters, private file metadata and Protect Devices view on 23 September 2026 | Protect advancing to the next command shows it accepted the rotation response. Native HTTPS login, adoption and later provisioning remain `needs_evidence`. |
| Two more no-camera probes accepted exact, independently implemented virtual settings. Protect sent `ChangeSoundLedSettings`, then timezone-only `ChangeDeviceSettings`; each was acknowledged only after mode-600 logical state was persisted. | Protect 7.2.105 AI Port settings modules `28175` and `71847`; pinned, payload-free counters and private file metadata on 23 September 2026 | The container has no physical LED or speaker. A reply proves protocol progression, not physical output. One unclassified control request remained; its role is `needs_evidence`. |
| With the settings sequence handled, a bounded single-Flur trial paired the G3 camera and decoded 129 frames. The control WebSocket did not reconnect during the observed interval. Protect sent an explicit stream stop when Flur was unpaired, and the candidate returned to passive mode with zero retained frames. Protect then showed the AI Port **Click to Pair**, Flur Online at HD/Adaptive, and the separate AI Key Online. | Pinned, payload-free stream counters and Protect Devices view on 23 September 2026 | Initial streaming was sustained for roughly one minute. This does not prove recovery across reconnect, adoption, smart event delivery or permanent AI Port operation. Those remain `needs_evidence`. |
| Protect's AI Port removal dialog described the existing record as one that would need readoption. A six-minute, no-camera reconnect test sent `Adopted: true` with the candidate's existing certificate to the pinned controller. Protect 7.3.60 accepted the WebSocket, replied to hello and sent parameter agreement. The private, controller-bound adopted record had mode 600 and no token. After the temporary window was removed and the container restarted, pinned health again showed an adopted connection, and Protect displayed AI Port **Online** with an Excellent connection while the separate AI Key remained Online. The temporary HTTPS 443 relay accepted zero connections. | Pinned candidate health, private file metadata, relay aggregate counters and read-only Protect Devices views on 23 September 2026 | Recovery of an existing adoption is native-verified. No management `POST` occurred, so fresh token-based adoption and native HTTPS login remain `needs_evidence`. No camera was paired, no stream was enabled, and no smart event was delivered in this trial. |
| With that recovered adopted connection, a fresh Flur pairing attempt reached `UiStreamControl` but was rejected because normal mode deliberately had no camera-ingress policy. A four-minute, Flur-only permit then rejected a stream-source setting copied from the camera address. The same permit, changed only to the pinned controller address, accepted the native stream command, decoded 38 frames, and sent `EventAIPortStatus`. Protect showed Flur under the AI Port with an AI badge. Unpairing sent an explicit stop; Protect returned to **Click to Pair** and Flur remained Online at HD/Adaptive. The private permit was removed and the container restarted: the adopted control connection remained up with camera ingress disabled and zero frames in memory. | Chrome Protect Devices view, certificate-pinned payload-free candidate counters and private config shape on 23 September 2026 | Native pairing and stream intake also work after existing-device recovery. In this installation the RTSP endpoint in Protect's command is on the controller, not the camera's own address. The fixed-source check prevented unintended stream access. This still does not prove fresh adoption, durable multi-camera operation, detection inference, native smart-event delivery, or alarms; all remain `needs_evidence`. |
| A later Flur-only diagnostic ran the optional CPU-only local detector in a 2 GiB Apple container. Protect showed Flur paired to the AI Port and Online. Pinned health recorded decoded stream frames and three attempted / three successful RF-DETR Nano model calls, with zero object observations and no detector error. After the test, Protect showed AI Port **Click to Pair** and Flur Online at HD/Adaptive; the AI Key and Giebel Vorn were still Online. The private diagnostic fields were removed and the passive image restarted with adoption and control connected, ingress off and zero decoded frames. | Chrome Protect Devices view, certificate-pinned aggregate health counters and private config-shape check on 23 September 2026 | This proves bounded live inference on a paired legacy-camera stream and rollback. Empty observations on three frames do not measure detection accuracy. No smart-detection readiness, native `EventSmartDetect`, original-camera timeline event, alarm, durable stream or sustained resource result was established; each remains `needs_evidence`. |

Protect 7.2.105's request helper logs in to a device with `POST /api/1.2/login`, using the current username and plain password, then carries a session cookie on later authenticated requests. Its `UpdateUsernamePassword` command carries a SHA-512 crypt hash of the new password. These static interface observations guided an independent implementation of private credential persistence and login. Protect's next command after the live rotation reply is now observed; native HTTPS login is still `needs_evidence`.

The initial discovery probe used a newly generated locally administered MAC and self-signed certificate, separate from the existing AI Key. It sent no token, default password, camera stream or model request. The later, separately gated stream trials are recorded above. Private instance configuration is not included in the public repository because it carries local addresses and a certificate pin.

An adopted, no-camera reconnect still counted one unknown controller request after the settings handshake. A four-minute probe retained only a short hash of its function name in memory. Matching that hash against names in the inspected controller package identified `UpdateFaceDBRequest`. Protect 7.2.105's recognition handler sends this command to connected AI Ports with a private face-database URL. The probe was removed and the container restarted with no diagnostic flags. A new candidate answered the next native request with an explicit unsupported status without fetching or echoing its payload; pinned health counted one response, and Protect still showed the AI Port Online. This identifies a face-database command, not proof that face recognition works. Protect gave no explicit acknowledgement of the unsupported result, so its handling and actual face recognition remain `needs_evidence`.

## Smart-event ingress gate

Static Protect 7.2.105 module `29116` associates an AI Port message with a paired camera by matching `payload.deviceID` to that camera's MAC. It routes `EventSmartDetect` edges `enter`, `moving`, `leave` and `none` to the original camera's event handlers, subject to the controller's normal filters. Its message-lateness check parses the envelope's `timeStamp`; the candidate now sends an ISO UTC value aligned with the event payload's `clockWall`. The same module maps per-camera `EventAIPortStatus.isSmartDetectReady` to smart-service readiness. Module `36276` sends enhanced-motion and smart-detection settings only when the paired stream is running and readiness changes to true; module `20274` builds a `ChangeSmartDetectSettings` payload for the AI Port when the camera supports smart detection. These are interface observations from a different Protect version, not live proof that 7.3.60 persists an emulated event.

In that static 7.2.105 module, the lateness check logs a warning for old `enter` messages but does not itself drop them. Module `77755` reads `clockWall` and computes the event start from that value and the camera's pre-padding setting. This supports a bounded historical-event probe as an interoperability experiment; it does not establish that Protect 7.3.60 will accept a backdated event or link it to retained footage.

The settings payload in that static version includes enabled object types, zone and line maps, event timing, recognition accuracy and an AI Port camera `deviceID`. Module `77755` skips an `enter` event if the camera cannot be resolved, no supported status map is present, or `clockWall` is invalid. Its event-type parser recognizes zone, line, loiter and tamper status maps. The candidate now checks a narrow full-frame subset for the exact camera and counts matches without retaining the nested payload. Its default and settings-only diagnostic still answer `ChangeSmartDetectSettings` with a fixed unsupported status. An expiring, one-camera event diagnostic acknowledged a validated person policy on Protect 7.3.60, but no model observation qualified for native event publication. A settings request cannot prove native event delivery or recognition.

The candidate reports streaming only after a decoded frame and reports smart and audio readiness false by default. Its passive profile does not emit `EventSmartDetect`. In a bounded single-G3 trial, reporting smart readiness alone caused Protect 7.3.60 to send `ChangeSmartMotionSettings` but no `ChangeSmartDetectSettings`. The candidate validated and acknowledged the motion command as a no-op. Static module `20274` checks `camera.featureFlags.hasSmartDetect`; module `28164` derives that flag from a non-empty `smartDetectTypes` list. This suggested a missing capability announcement, but the static version alone did not prove the live cause.

On 23 September 2026, two exported frames from one existing G3 Instant motion recording, captured two seconds apart, were processed locally with the checksum-pinned RF-DETR Nano checkpoint. The detector returned one person observation in each frame (confidence 0.949 and 0.954), and the project's two-hit temporal tracker emitted one person enter. The source JPEGs and object coordinates were not committed. No AI Port event was sent to Protect in that check, so original-camera timeline persistence, historical timestamp acceptance and notification behavior remain `needs_evidence`. The separate one-use recorded-event diagnostic can test those native gates after the runtime is available.

Static Protect 7.2.105 module `37750` requires `displayTimeoutMSec` for object-track updates on both enter and leave. Its separate leave handler accepts a smaller payload, but it also passes that payload to the object-track parser before closing the event. The candidate now includes the bounded display timeout on both edges so its leave update meets that parser's required field shape. This is static compatibility evidence and a synthetic regression check, not a Protect 7.3.60 timeline result.

A follow-up six-minute, single-G3 trial sent a per-camera `EventFeatureFlagsUpdated` with one person capability before the smart-ready status. Protect 7.3.60 then sent `ChangeSmartDetectSettings` twice for the original camera; its sanitized policy shape contained zero enabled detection types. Protect Recording Manager showed **Record AI Events** disabled for that camera. After enabling it, Protect sent another `ChangeSmartDetectSettings` with one enabled type. The candidate still returned unsupported and did not emit an object event. The camera was unpaired, returned Online at HD/Adaptive, and the AI Port restarted with a private base configuration, ingress off and no probe flags. The separate AI Key remained Online. This verifies the capability and camera-policy gates; it does not verify event persistence, model accuracy, recognition, alarms, or all-camera operation. The next native check needs a validated settings handler and one model-supported event that appears on the original camera timeline, followed by a verified test alarm. Protect 7.3.60 event payload acceptance, timestamp handling and recording association remain `needs_evidence`.

A further bounded G3 test enabled **Record AI Events** and the expiring person-event diagnostic. The candidate decoded the stream and received four `ChangeSmartDetectSettings` requests, each with one enabled detection type. Its sanitized policy shape reported person reverification enabled, so it rejected all four settings, made zero detector calls and emitted zero smart events. The Protect Intelligence UI showed the Person checkbox off; selecting all cameras except this G3 for Second Stage Verification changed the wire shape for Vehicle and Animal to disabled but left Person enabled. This is an observed UI/wire contradiction, not evidence that person reverification is safe to ignore. The global Second Stage setting was restored to **All** and verified after reload. The G3 was unpaired, Protect showed AI Port **Click to Pair** and the separate AI Key **Online**, and the private diagnostic was removed. The container restarted passively; pinned health showed adopted control connected with zero stream and detector activity. Native event acceptance and the reason for the person-policy mismatch remain `needs_evidence`.

Static Protect 7.2.105 module `10224` builds per-class reverification settings from enabled AI policies. Its custom policy includes integer `minPresenceProbability` and `maxPresenceProbability` values on a 0–100 scale; the default range is 40–80. The candidate now validates that narrow envelope and drops person observations at or below the upper bound before tracking. This is a conservative suppression of uncertain detections, not an implementation of second-stage verification. In a new bounded Protect 7.3.60 G3 pairing, enabling **Record AI Events** produced a one-type settings request; the candidate acknowledged the exact policy and Protect kept the camera paired. The prior request with zero enabled types was rejected. A stream reconnect then let the local detector complete at least 100 model calls on decoded frames without an error. Those calls yielded zero supported objects and zero native smart events, so Protect's event acceptance remains `needs_evidence`.

A second bounded pairing used a G5 camera with a person visible in Protect. Protect sent one configured smart zone, which the candidate rejected because it cannot yet enforce zone geometry or event-zone semantics. With the event permit removed, a three-frame local-only check completed three model calls, recorded three supported object observations and one internal track. The aggregate counters do not identify the object class; the visible person makes that interpretation plausible but unproven. No native smart event was sent. Both test cameras were unpaired, the private stream/model permits were removed, and the AI Port restarted adopted and connected with zero stream or detector activity. The separate AI Key remained Online in Protect. Zone enforcement, original-camera event persistence, alarms and all-camera operation remain `needs_evidence`.

Static Protect 7.2.105 modules `20274`, `6357`, `95627` and `25563` show primary smart zones keyed by numeric ID, a flattened 0–1000 coordinate polygon, object types and sensitivity. Module `286` recognizes a `zonesStatus` map as a smart zone event, and the status schema accepts `enter` and `leave` labels. An independently written parser validates simple primary polygons and attaches numeric zone IDs to the object descriptor and status map. Protect 7.3.60 has since acknowledged the candidate's validated one-camera Person zone request, and a live event appeared under All Detections. Person search indexing remains unverified. The parser now uses a provisional 90% box-area overlap rule after an approved recorded Person was excluded solely because its box crossed the lower edge of a nearly full-frame zone. Vertices within 5% of the frame border now count as on the border, after a cat along the bottom edge of Flur's default-inset zone overlapped it by under 50%. This is an inference from [Ubiquiti's stated LPR unlock overlap](https://community.ui.com/questions/AI-Pro-License-Plate-Unlock/fccc7c12-3a6d-4efe-9678-5bd8db1b88d6), not a confirmed AI Port Person threshold. Sensitivity mapping and persistent Person classification remain `needs_evidence`.

A subsequent one-use native trial on Protect 7.3.60 paired Flur (G3 Instant) with the adopted candidate and enabled **Record AI Events** for that camera. Two owner-only JPEGs exported from its 19:01 recording, captured two seconds apart, produced two person observations with the pinned local RF-DETR Nano model; the second scored 0.954. With a current one-type person policy and a decoded native stream, the candidate durably claimed its one-use permit and sent one `EventSmartDetect` enter/leave pair at the recording time. Pinned, payload-free health counters recorded one qualified and claimed probe, one enter and one leave, with no probe error. A read-only Protect subscription observed one targeted smart-video add and one update, but zero person adds. Flur Playback showed one **Smart** card at 19:01, including after a full page reload while the camera was paired. This is evidence that a model-backed native event reached the original camera's Smart timeline in this bounded historical test. It does not prove the class Protect assigned, alarm delivery, event durability beyond the observed reload, live-stream event generation, multi-camera operation or AI Port feature parity; those remain `needs_evidence`. The private frame files and model output were not committed. After the trial, **Record AI Events** was disabled, Flur was unpaired and stayed Online without the AI badge, and the candidate restarted with only its passive configuration. Protect showed the AI Port and separate AI Key Online; pinned health showed the adopted control connection active and ingress disabled. The historical recording still showed two 19:01 entries under **All** after rollback, but their exact classes remain `needs_evidence`.

A later read-only Find Anything check, after Flur was unpaired and AI Event recording was disabled, showed two Flur cards around 19:01 under **All Detections**, including the historical card without a confidence label. Filtering the same camera to **Person** returned zero results. Static Protect 7.2.105 module `77755` creates the event with an empty `smartDetectTypes` list, then module `11141` updates that list from validated track descriptors. The first recorded probe sent enter and leave back-to-back despite timestamps two seconds apart. The candidate now preserves two seconds of real delivery spacing and verifies that the same stream, policy and permit remain active before leave.

In a second bounded test on Protect 7.3.60, the same private Flur recording and pinned model again produced one qualified, claimed Person event. The candidate reported one enter and one leave with no probe error. Flur was paired to the AI Port and **Record AI Events** was enabled. Yet Find Anything, filtered to Flur and **Person**, showed **0 Results while paired**. This falsifies pacing alone as a sufficient class-indexing fix; it does not identify whether the issue lies in the event envelope, track descriptors, Protect policy, search index or historical timestamp handling. The AI recording setting was restored, Flur was unpaired, and the candidate restarted passively with ingress disabled. Full class indexing and alert parity remain `needs_evidence`.

A third bounded Flur test used a new one-use permit and the same owner-only historical frames. The candidate reported one model-qualified Person enter, one leave and zero probe errors. Protect displayed another Smart card at the original time, while the Flur Person filter returned zero results during pairing. The pinned, read-only integration subscription observed one targeted smart-video add and one update; **both explicitly contained `smartDetectTypes: []`**, with no malformed messages or Person counts. This distinguishes the empty class list from a partial update that merely omitted the field. It does not show why Protect 7.3.60 left that list empty. Afterward **Record AI Events** was disabled, Flur returned to **Click to Pair**, and certificate-pinned candidate health after a passive restart showed an adopted control connection, no enabled ingress and zero decoded frames. The original camera remained Online. Private frames, identifiers and credentials were not committed.

The private 7.3.60 support archive also logged `BAD_REQUEST` warnings that this exact event's start and end reached `eventTracker` late. This warning must not be read as an event rejection: in the locally inspected 7.2.105 controller, `checkAndLogEventLateness` constructs a `BadRequestError` for logging and returns without throwing, and the `EventSmartDetect` enter/leave routing path calls its timing logger before publishing the event. The installed 7.3.60 implementation may differ; its warning alone does not establish that lateness caused the empty class list. In the 7.2.105 path, `onObjectEnter` calls `onObjectsUpdated`, which derives `smartDetectTypes` from nonstationary descriptor `objectType` values. The live 7.3.60 path from descriptor to saved class remains `needs_evidence`. Another historical Smart card alone cannot serve as a Person-indexing acceptance test.

On 24 September 2026, a fourth bounded Flur replay used the same two private recorded frames, 2 seconds apart. The pinned RF-DETR Nano model scored the single tracked person at 0.949 and 0.954, with different observed positions. The revised one-use candidate sent enter at the first capture time, moving at the second capture time and leave two seconds later. Certificate-pinned health recorded 38 decoded frames, one qualified and claimed probe, one of each edge and zero probe errors. Protect showed Flur paired and Online with its AI badge and **Record AI Events** enabled. The Flur **Person** filter still showed **0 Results while paired**. Under **All Detections**, Protect showed five 19:01 Flur cards, but this visual count alone cannot attribute the newest card or its class to the replay. The read-only event subscription remained open past its requested 180-second deadline and was interrupted, so its add/update and `smartDetectTypes` counts are `needs_evidence` for this trial. The watcher completed normally in a later one-second no-event control check. Flur's AI Event recording was then restored to Disabled and its AI Port pairing removed; Protect showed AI Port and Flur Online, and the candidate restarted with only the five-field passive config, zero ingress frames and an adopted control connection. The moving edge did not establish Person indexing, so legacy-camera feature parity remains open.

The same inspected controller routes `EventSmartDetect` `moving` messages to its object-update service. The bounded candidate now encodes a rate-limited moving descriptor for a confirmed active live track, keeps each camera's tracker and zone policy isolated, and withholds the update when the source stream is absent. Synthetic tests cover enter, moving, leave and policy revocation. The recorded-frame moving update was sent during the fourth Protect 7.3.60 trial above, but it did not produce a Flur Person result. Live-track moving updates and their saved class remain `needs_evidence`.

On 24 September 2026, a Flur pairing attempt against the passive candidate failed because it rejected two native stream-control requests with no ingress policy. A new private, single-camera `paired_stream` policy removed the diagnostic time limit for that exact camera while leaving inference and AI event publication disabled. After rebuilding and replacing the container with the same adopted identity, Protect showed the AI Port and Flur Online, Flur had the AI badge and appeared under the AI Port's Paired Camera list. Certificate-pinned health reported one accepted stream control, zero rejections, one active stream with decoded frames, and zero native smart events. The camera was left paired. This establishes a stable baseline for subsequent class-indexing work without repeated unpairing; it does not establish Person indexing, alert delivery, or AI Port feature parity.

## Capacity and container listeners

[Ubiquiti's AI Port FAQ](https://help.ui.com/hc/en-us/articles/28315005177239-Protect-AI-Port-FAQs) gives per-device limits by source and resolution and forbids mixing ONVIF and Protect cameras on one AI Port. The independently written `aikey.aiport_deployment` planner reads the existing private camera preflight, groups connected cameras by source, and reserves capacity using those published limits. Its default scope selects connected cameras without onboard smart detection. An explicit `legacy-and-g3-g5` scope also plans connected Protect G3–G5 cameras that already report smart detection, for optional AI Port enhancement. It excludes offline cameras and G6 cameras with onboard smart detection. For an unknown recording resolution, exact known Protect models use the camera's published maximum pixels as a capacity upper bound; unknown models still reserve the maximum per-device capacity. The plan retains `resolution_unverified: true`, and the live listener checks the actual stream dimensions against its ten-point limit. It never auto-pairs a camera.

On 24 September 2026, a certificate-pinned, read-only Protect 7.3.60 inventory reported 11 cameras: two without onboard smart detection and seven additional connected G3–G5 enhancement candidates. The public integration camera-detail response for a sampled G3 camera had no channels or recording-resolution field. The planner therefore reserved five AI Port instances under its 4K fallback, with only the existing first instance addressed; four more distinct, reserved LAN addresses remain `needs_evidence`. This is a capacity plan, not a deployment or proof that all nine cameras can be paired. A verified resolution source could reduce the count, but camera model or snapshot size alone cannot establish the active recording stream resolution.

On 25 September, a fresh pinned inventory still selected nine connected G3–G5 cameras. The five-instance fallback contradicted four already paired and decoding AI Port instances. The new exact-model upper bounds assign two capacity points to the published 1920×1080 [G3 Instant](https://techspecs.ui.com/unifi/physical-security/uvc-g3-ins); five points to the 2688×1512 [G4 Instant](https://techspecs.ui.com/unifi/physical-security/uvc-g4-ins), [G4 Bullet](https://techspecs.ui.com/unifi/physical-security/uvc-g4-bullet), [G4 Dome](https://techspecs.ui.com/unifi/physical-security/uvc-g4-dome?subcategory=all-physical-security) and [G5 Flex](https://techspecs.ui.com/unifi/cameras-nvrs/uvc-g5-flex?subcategory=all-cameras-nvrs); and five points to the 3840×2160 [G4 Pro](https://techspecs.ui.com/unifi/physical-security/uvc-g4-pro). The [G4 Doorbell Pro](https://techspecs.ui.com/unifi/cameras-nvrs/uvc-g4-doorbell-pro?subcategory=all-cameras-nvrs) has two lenses. It kept the conservative five-point fallback until the AI Port's lens behavior was verified. On 26 September, Protect 7.3.68's support log showed every `UiStreamControl` for the paired doorbell requesting one 1600×1200 main-lens stream, and the package lens stayed camera-owned (`secondLensZones`, processed by the camera). The doorbell now uses two points. By the ingress rule, the live streams of all nine cameras cost 2+2+5, 5+2, 5+5 and 5+5 points: 9/10, 7/10, 10/10 and 10/10. They do not establish the active recording resolutions or native-event delivery. The earlier five-instance result remains the historical output of the old planner.

Every planned AI Port needs its own identity, persistent state and reachable host IP. HTTPS 443 is a **fixed listener per AI Port instance**, not a new TCP port for each camera. The existing AI Key keeps its own HTTPS 8080 listener. A host-side UDP 10001 responder can cover multiple identities if discovery proves necessary, since Apple container's published UDP port does not deliver the LAN multicast sender reliably. The observed outbound controller WebSocket uses TCP 7442, and the tested Protect-camera stream uses outbound RTSP on TCP 7447. Plan schema v2 reports TCP 7447 only for Protect legacy cameras; ONVIF stream ports and any additional listeners remain `needs_evidence`. The planner does not claim a complete publish list before those are verified.

Run the read-only planner with an inventory from `local-aikey inventory`:

```sh
local-aiport-plan --inventory PRIVATE_CAMERA_PREFLIGHT_JSON \
  --camera-scope legacy-and-g3-g5 \
  --ai-key-ip AI_KEY_LAN_IP --ai-port-ip FIRST_AI_PORT_LAN_IP
```

For current automatic camera detection, the planner can fetch Protect's validated inventory directly through the pinned, read-only integration API:

```sh
local-aiport-plan --controller CONSOLE_PRIVATE_IPV4 \
  --api-key-file PRIVATE_PROTECT_API_KEY_FILE \
  --web-trust-file PRIVATE_WEB_TRUST_JSON \
  --web-cert-file PRIVATE_PINNED_WEB_CERT \
  --camera-scope legacy-and-g3-g5 \
  --ai-key-ip AI_KEY_LAN_IP --ai-port-ip FIRST_AI_PORT_LAN_IP
```

The JSON separates no-onboard-smart cameras from optional G3–G5 enhancements and names the number of instances and Apple container TCP publish bindings. Unassigned instances have no binding, so a deployment controller must refuse to start them until it has distinct, conflict-checked IPs. A container cannot add host-published ports to itself after startup; a host deployment controller must refresh inventory and create or reconcile the required instances. The current planner and diagnostic multi-camera candidate are not yet a production AI Port profile or an automatic container launcher.

Pass a previous private plan with `--previous-plan PRIVATE_PREVIOUS_PLAN_JSON` on later runs. The planner keeps each existing slot's camera order, source and host address, then puts new eligible cameras into remaining capacity or new slots. It refuses to reshuffle an assigned camera if that camera disappears, becomes ineligible, changes source or raises a slot above capacity. This is a dry run: it does not check whether an address is bound on the host, create an instance or change a Protect pairing. Keep previous plans private because they contain camera IDs and LAN addresses. Host listener checks, safe removal and automatic apply remain open in [#45](https://github.com/Olli0103/unifi-ai-key-emulator/issues/45).

After assigning a distinct private LAN address to a slot, `local-aiport-provision` can create its own persistent MAC, certificate, pinned controller trust and mode-600 base config in a new mode-700 state directory. It verifies an existing directory without rotating any identity material, and refuses incomplete or conflicting state. The plan, controller certificate and its SHA-256 pin must be supplied from private files and independently verified trust. For example:

```sh
mkdir -m 700 PRIVATE_AI_PORT_STATE_PARENT
local-aiport-provision --plan PRIVATE_PREVIOUS_PLAN_JSON --slot SLOT_NUMBER \
  --state-dir PRIVATE_AI_PORT_STATE_PARENT/slot-SLOT_NUMBER \
  --controller CONSOLE_PRIVATE_IPV4 \
  --controller-cert-file PRIVATE_PINNED_CONTROL_CERT \
  --controller-pin VERIFIED_CONTROL_CERT_SHA256
```

Provisioning does not assign an IP, publish HTTPS 443, adopt a device or pair cameras. It leaves an interrupted, incomplete directory untouched for explicit recovery. The independent [host relay](../aiport-candidate.md) now validates a complete addressed plan and every existing instance identity, pins each candidate's upstream TLS endpoint, and rolls back newly opened port 443 listeners after a failed bind. A read-only preflight succeeded against the running Mac candidate. The relay has only synthetic listener tests; multi-address routing, persistent service management, NAS network mode, adoption and automatic camera reconciliation still require native qualification under [#45](https://github.com/Olli0103/unifi-ai-key-emulator/issues/45).

The [isolated candidate service](../aiport-candidate.md) supplies HTTPS management, a persistent device WebSocket and opt-in single-camera or multi-camera stream intake. A host relay can supply transport to port 443 for addressed instances, but it has not been deployed as a permanent service. Frame counters and a small allowlist of command names retain neither frame bodies nor credentials. The management endpoint remains relevant to fresh adoption and needs a separate diagnostic. Camera pairing waits for a decoded frame and an accepted stream-control response. [Ubiquiti's port reference](https://help.ui.com/hc/en-us/articles/218506997-Required-Ports-Reference) identifies 7447 as RTSP output; the live G3 trial decoded video through that outbound connection.

An independent `AiPortIngressPool` routes stream-control commands to up to five explicitly allowed cameras and enforces a ten-point budget from the requested pixel dimensions, including a stalled decoder's reserved points. It rejects unlisted camera IDs, source-address changes and over-capacity starts. Both expiring diagnostics and a persistent live profile wire the pool into the adopted candidate's stream-control, list, reset and per-camera status paths. The native three-camera test below verifies pairing and streaming; original-camera events from all three remain open.

The bounded multi-camera inference path now revokes one camera's policy and emits a closing event when that camera's model budget ends or inference becomes unavailable, before reporting smart readiness false. A synthetic two-camera test confirms the other camera's policy remains active. Native multi-camera delivery and correct original-camera timelines remain `needs_evidence` under [#79](https://github.com/Olli0103/unifi-ai-key-emulator/issues/79).

On 24 September 2026, the user's fresh walk through Flur produced the first sustained live result that Protect indexed as **Person**. Before the container image change, certificate-pinned health showed one model-backed smart enter and leave, two snapshot requests and two successful uploads, with no detector error. Protect 7.3.60 Find Anything, filtered to Flur and Person, showed a new 08:46:08 result with 94% confidence. The same result remained after a full page reload. Flur is a G3 Instant without onboard smart detection, and its paired AI Port was the only configured local Person detector. This supports live original-camera Person indexing in this one case; it does not establish alarm delivery, accuracy or other classes.

The same adopted identity then ran the merged live pool image without unpairing Flur. Protect paired a second G3 Instant and a G5 Flex to it. Its device panel listed all three cameras, and certificate-pinned health reported three active streams with decoded frames, a connected controller, and 241 successful shared-model calls with zero failed cameras at the final read. The second G3's **Record AI Events** setting was enabled for Person after pairing. No native Person result has yet been verified for that camera or the G5 Flex. A G4 Instant was selected as a fourth camera, but its stream-control request exceeded the candidate's ten-point limit twice; Protect did not add it, while the first three cameras stayed paired. The candidate's limit uses the stream dimensions in the native command, which can cost more than the UI's nominal "2K" category suggests. Another AI Port identity and LAN address are required for that camera at its current stream dimensions. No camera was unpaired during this trial.

After a later Flur walk, Protect still showed six Person results, with none newer than the earlier 10:51 CEST result. The live pool continued decoding and making successful model calls. The decoder was supplying one frame per second to a tracker that requires two matching observations, a plausible cause of missed short crossings but not a proven explanation for this particular walk. The decoder was raised to two frames per second and deployed with the same adopted state. Over a ten-second check, three paired streams yielded 60 decoded frames and 60 successful model calls with no dropped frames. Protect still showed the AI Port Online with Flur, Schlafzimmer and Büro paired. Improved Person recall after this change remains `needs_evidence`.

After updating the live pool to recover accepted decoders that exit unexpectedly, the three cameras remained paired. During a later G3 Person trial, an initial Protect check showed no new result, while the shared model had completed 2,099 of 2,100 calls since restart and all three streams had decoded frames. A subsequent pinned health read showed four smart enters, one moving update, four leaves and four snapshot uploads, with three active decoded streams and no decoder restart. On a fresh Protect Find Anything reload, that camera's Person filter increased from four to six results. The two new results scored 93% and 92%, ten seconds apart. Both opened the original camera's event detail and linked to its playback; the playback page loaded video and displayed both Person thumbnails. This verifies live G3 Person indexing and playback association during a three-stream run. The aggregate health counters cannot attribute every event to a camera, and one successful walk does not establish recall, alarm delivery, continuous multi-camera indexing or parity. Results from the other two paired cameras remain `needs_evidence`.

After the 24 September code-only tracker update, the same adopted Mac identity reconnected without re-pairing and resumed three decoded streams. Its pinned health after 112 seconds reported 658 decoded frames, 194 successful shared-model calls and 450 superseded frames, with 65 attempts per paired camera and no failed camera. The interval includes model warmup, so it is not a steady-state benchmark. It does show that decoding at two frames per second per camera does **not** mean this CPU-only detector processed every frame; short-event recall and hardware capacity remain `needs_evidence`.

On 25 September 2026, the API-backed live pool reported Person observations and one native smart enter/leave pair for each of two paired cameras, a G5 Flex and a G3 Instant, with no API failures. At matching times, Protect 7.3.68 Find Anything showed a **Person** result for each camera, at 99% and 94% confidence. Each result opened playback for the matching original camera; both remained in the filtered results after a full page reload. The G3 Instant has no onboard smart detection, so its result supports a live API-backed, persisted Person event on the original camera. The G5 result is also correlated with a candidate event, but Protect does not expose per-event source provenance here, so independent attribution remains `needs_evidence`. Recall, alert delivery, other classes, the remaining paired cameras, and sustained all-camera operation also remain open.

Later that morning, without re-pairing, pinned Mac AI Port health rose from four to six smart enter/leave pairs: Flur rose from three to four and Büro from one to two. Protect's Person filter then showed a new Flur result at 97% and a new Büro result at 99%, each on its original camera. The other paired G3, Schlafzimmer, had three score-eligible Person observations but zero zone-eligible observations or smart enters; its Person filter showed zero results. This confirms repeat native Person indexing for Flur and Büro while leaving Schlafzimmer's zone rejection and recall `needs_evidence`.

The NAS slot 2 camera streams both decode, but Esszimmer has no enabled smart policy. A read-only download of its deployed `config.json` matched the local `config.package-staged.json` byte for byte; that deployed file advertises only `package`, whereas the local intended config advertises `person`, `vehicle`, and `animal`. Protect's integration row for Esszimmer currently reports `smartDetectTypes: [package]` and `smartDetectSettings.objectTypes: []`, and the candidate labels the incoming empty policy `disabled_policy`. A four-class union config for `person`, `vehicle`, `animal`, and `package` was validated locally but has not been activated. Changing advertisement alone cannot prove Protect will enable Person or save events. The same slot's doorbell still rejects a non-empty secondary-lens package zone, which the class-list change does not address. The other two NAS slots retain decoded streams and accepted policies but have no native smart enters yet.

A later pinned read of the Doorbell Pro's integration row returned no channel list. A bounded pinned request to the controller bootstrap endpoint returned HTTP 500, so it yielded no lens-to-stream mapping. The non-empty secondary-lens Package zone therefore remains rejected; mapping it to the decoded primary stream would be an unsupported inference. In the Mac three-camera pool, one G3 stream intermittently stopped decoding while the other two continued. Aggregate health reached 16 decoder restart attempts and one success, with no stderr marker. That does not distinguish a source timeout from a retry-path fault. The candidate now exposes only fixed per-camera recovery categories in private health; the new diagnostic image reconnected with all three streams, but a subsequent failure observation is still `needs_evidence`.
